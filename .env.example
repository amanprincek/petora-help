# Frontend config (optional).
# Leave empty to use the same origin (Vite proxies /api → localhost:4000
# in dev; the backend serves the built site in production).
# Set this ONLY if the API runs on a different origin, e.g.:
# VITE_API_URL=http://localhost:4000

VITE_API_URL=
